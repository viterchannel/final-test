The biggest mistake is sending an OTP before checking if the user exists or if that number is already linked to a Google account. We need a Unified Auth Gatekeeper.The Workflow:Identifier Input: User enters something (Phone, Email, or Username).Account Discovery: The system checks the database: "Does this identifier exist?"Method Enforcement: * If the user exists and has Google Linked, the system forces "Login with Google."If the user is new, it triggers the Registration Flow.Admin Toggle Check: The system only shows options (Magic link, 2FA, etc.) that the Admin has enabled in the backend.2. Preventing Multi-Accounts & Fake UsersTo stop people from creating 10 accounts with different methods, you must implement Account Linking.Primary Key: Use a unique internal UUID, not the phone number or email.Verification Wall: Even if a user logs in via Facebook, the system must check if a verified phone number is attached. If not, redirect to a "Verify Phone" screen before they can use the app.Device Fingerprinting: Store the device_id. If one device tries to register 5 different accounts, the Admin panel should auto-flag or block them.3. Step-by-Step Implementation StrategyPhase A: The Unified User SchemaInstead of separate tables for different apps, use one Central Users Table with roles.user_id (Primary)email, phone, username (Unique)auth_provider (Local, Google, Facebook, etc.)is_verified (Boolean)role (Customer, Vendor, Rider)two_factor_enabled (Boolean - Admin controlled)Phase B: The "Smart" Login LogicInstead of a "Login" button and a "Register" button, use a single "Continue" flow:Admin Config Fetch: App calls GET /auth/config. Admin says: "Only Phone and Google are allowed."Input: User enters +92300...Backend Check: * If phone exists: Send OTP $\rightarrow$ Verify $\rightarrow$ Check if 2FA is on $\rightarrow$ Login.If phone NOT exists: Start Registration Flow $\rightarrow$ Ask for Name/Email $\rightarrow$ Verify $\rightarrow$ Create Account.Phase C: Global Admin ControlsThe Admin Panel needs a "Security Settings" page where you can toggle:Enable/Disable: Google, Facebook, Magic Link, 2FA.Session Management: "Logout from all devices" feature.KYC Requirement: (Especially for Riders/Vendors) to prevent fake accounts.4. Fixing the "OTP without Account" BugYou mentioned that even if a user isn't registered, an OTP is sent. This is a waste of SMS costs and a security risk.The Fix:Rate Limiting: Limit OTPs to 3 per hour per IP/Device.Validation: The backend should validate the format before calling the SMS Gateway.User Check: ```javascriptif (admin.require_registration && !userExists) {return error("Account not found. Please sign up.");}5. Multi-App Integration (Customer, Rider, Vendor)To avoid duplicate code, you should build a Shared Auth Microservice or a shared library.Next Steps for your Code:Refactor Database: Ensure Email and Phone have UNIQUE constraints.Cleanup Functions: Remove loginWithPhone, loginWithEmail, registerUser. Replace them with one authenticateUser(identifier, type) function.State Management: Use a global state (Redux/Provider) to track auth_status. If a user is "Partially Authenticated" (logged in but needs 2FA), don't let them see the Dashboard.